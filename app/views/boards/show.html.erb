<%= content_tag(:body, "", id:"root", data:{
  controller:"boards-react",
  db:{
    board: {
      id: @board.id,
      name: @board.name,
      description: @board.description
    },
    currentMemberId: @member.id
  }
})%>