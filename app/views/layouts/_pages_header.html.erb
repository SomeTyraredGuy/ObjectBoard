<div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <h1 class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-decoration-none text-white"><%= link_to "ObjectBoard", root_url, class: "nav-link text-light"%></h1>
        <nav>
          <ul class="nav nav-pills">
            <li class="nav-item"><%= link_to t(:pricing), pricing_url, class: "nav-link text-light"%></li>
            
            <% if user_signed_in? %>
              <li class="nav-item"><%= locales_selector %></li>
              <li class="nav-item"><%= link_to t(:boards), boards_url, class: "nav-link text-light"%></li>
              
              <!-- TODO: додати менюшку взаємодії з профілем -->
              <li class="nav-item"><%= link_to t(:logout), destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link active"%></li>
            <% else %>
              <li class="nav-item"><%= link_to t(:login), new_user_session_path, class: "nav-link active"%></li>
              <li class="nav-item"><%= link_to t(:sing_up), new_user_registration_path, class: "nav-link active"%></li>
            <% end %>
            
          </ul>
        </nav>
    </header> 
  <div>